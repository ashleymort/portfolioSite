var x = 5;
var y = 6;
var z = x + y;
document.getElementById("demo").innerHTML = z;

// all javascript is case senstitive!!!add comment before code

"John" + "" + "Doe"

/*concatination adds 1 x string to another "" = space between

var = variable
for marks a block of code to be executed as long as the condition is true
if...else - marks a block of code to be excecuted based on a condition
return - exits the function
break - terminates a switch or loop
function - declares a function

you can not use these for the name of a variable, object or function and are case sensitive*/

//use camelCase dont capitilize first word ex( myFunction, addNumbers, changeCustomerInfo etc)

/* put spaces after operators , = + etc always use indenation for code blocks 

    var person = {
        firstName: "John",
        lastName: "Doe",
        age: 50,
        eyeColor: "blue"
    };

    var multiValues [50, 60, "text", 14, "more text"] = array starts at 0 so 1 =0
   
    document.getElementById("mainTitle").addEventListener("click", function() {alert("Hello");});
    document.getElementById("mainTitle").addEventListener("mouseover", function() {alert("Hello,Again");});

*/